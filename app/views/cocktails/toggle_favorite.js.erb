var cocktailId = '<%= @cocktail.id%>'

function replaceLikeButton(cocktailId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${cocktailId}']`)
  likeButton.classList.toggle('red');
}


replaceLikeButton(cocktailId);

