<div class="container">
  <h1>Cocktails</h1>
  <hr>
  <div class="row">
    <div class="col-xs-12 col-sm-4">
    </div>
    <div class="col-xs-12 col-sm-8">
      <% @cocktails.each do |cocktail| %>
        <h3><%= cocktail.name %></h3>
        <p><%= cocktail.preparation %></p>
        <p><%= link_to "Like", toggle_favorite_cocktail_path(cocktail), remote: true, method: :post %></p>
      <% end %>
    </div>
  </div>
</div>